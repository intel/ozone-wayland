# -*- mode: python -*-

import os

def jhbuilddir():
  return os.path.dirname(os.path.realpath(__file__))

use_local_modulesets = True
moduleset = jhbuilddir() + '/wayland.modules'
modules = ['weston']
checkoutroot = jhbuilddir() + '/../../../out/wayland/source'
prefix = jhbuilddir() + '/../../../out/wayland/root'
autogenargs=''

os.environ['EGL_PLATFORM'] = 'wayland'
